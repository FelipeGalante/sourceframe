---
title: "PathMerit Master Source of Truth"
type: "site-index"
source: "PathMerit_Domain_Navigation_Source_of_Truth_DATABASE_REWORKED_CLEAN_COLUMNS.html"
---

# Site Overview

Domain-based HTML workspace

# One master file, separated into working domains.

This version keeps the extensive source-of-truth content, but no longer presents everything as one continuous page. Use the high-level domain tabs, then the secondary menu inside each domain. The full archive view remains available when you need to print, scan, or export everything at once.

**5**domains

**17**canonical sections

**479k**source chars preserved


## Sample layout

The public sample site stays intentionally small so the template remains easy to swap over. The private PathMerit fixture content used for routing tests now lives in a separate project root and is not part of the public build.
